openapi: 3.0.0
info:
  title: OpenAPI Spec test
  description: OAS v3.0 테스트
  version: 0.1.3
servers:
  - url: http://api.example.com/v1
    description: Optional server description, e.g. Main (production) server
  - url: http://staging-api.example.com
    description: Optional server description, e.g. Internal staging server for testing
paths:
  /puppy:
    get:
      summary: 강아지 목록을 리턴합니다.
      description: 강아지 목록을 리턴합니다.
      responses:
        '200':    # status code
          description: A JSON array of puppy
          content:
            application/json:
              schema: 
                type: array
                items:
                  type: string